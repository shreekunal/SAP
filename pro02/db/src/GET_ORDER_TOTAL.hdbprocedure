PROCEDURE "GET_ORDER_TOTAL" (
   IN IV_ORDER_ID NVARCHAR(36), 
   OUT OV_TOTAL_COST DECIMAL(15, 2) 
) 
LANGUAGE SQLSCRIPT 
SQL SECURITY INVOKER 
AS 
BEGIN 
   SELECT COALESCE(SUM(PRICE * QUANTITY), 0) INTO OV_TOTAL_COST 
   FROM "MY_ORDERSHOP_SALESORDERITEMS" 
   WHERE PARENT_ID = IV_ORDER_ID; 
END